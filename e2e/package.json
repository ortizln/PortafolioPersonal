{
  "name": "portfolio-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Pruebas E2E (Playwright) para el sitio ALANTEK",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
